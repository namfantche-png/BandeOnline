node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Uploads locais (fallback quando Cloudinary não está configurado)
# IMPORTANTE: Use apenas /uploads/ para ignorar a RAIZ
# backend/src/modules/uploads/ é CÓDIGO e deve estar no Git!
/uploads/
uploads.db
*.temp
